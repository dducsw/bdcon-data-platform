# Data Services

This folder contains active Kubernetes manifests for core data services used by the new layout: PostgreSQL, MinIO, Redis, Kafka, Hive Metastore, and Gravitino.

Nested duplicate folders inside service directories are legacy migration artifacts kept temporarily for rollback and should not be referenced by new changes.

