# Platform Services

This folder contains active manifests for platform-facing services such as Spark, Superset, and ingress resources.

Use the top-level files and folders here for new work. Nested duplicate directories are retained only as rollback artifacts during migration.

