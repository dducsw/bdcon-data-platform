# Scripts

Use grouped script folders for active workflows:
- bootstrap/ for operators and first-time setup
- deploy/ for cluster lifecycle commands
- verify/ for checks and utility reads
- cleanup/ for teardown helpers

Legacy top-level script copies remain temporarily for rollback only.

